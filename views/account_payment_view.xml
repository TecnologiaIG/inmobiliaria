<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_payment_form" model="ir.ui.view">
        <field name="name">account.payment.form.inherit.inmobiliaria</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="sale_id"/>
                <field name="descripcion"/>
                <field name="cheque"/>
                <field name="boleta"/>
                <field name="fecha_boleta"/>
            </field>
            <!-- Flujo legacy de anticipos reemplazado por el catálogo de tipos
                 (account_advance_payment). El campo se conserva por los pagos
                 históricos pero ya no se ofrece en el formulario. -->
            <field name="payment_type" position="after">
                <field name="es_anticipo" invisible="1"/>
            </field>
        </field>
    </record>
</odoo>